apiVersion: v1
kind: Service
metadata:
  name: kargo-demo
  labels:
    app: kargo-demo
spec:
  selector:
    app: kargo-demo
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
